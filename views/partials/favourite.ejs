<form action="/favourites" method="POST">
    <!-- Hidden input for home ID -->
    <input type="hidden" name="id" value="<%= home._id %>">

    <button 
    type="submit"
    class="w-full border border-[#0F172A] text-[#0F172A] py-2 rounded-md hover:bg-[#0F172A] hover:text-white transition duration-300"
>
    Add to Favorites
</button>
</form>